name: Build Vex Agent Runtime v0.11.7.77 Window Control Broker

on:
  push:
    branches: [ "build/v11777-window-control-broker" ]
    paths:
      - "Tools/apply_v11777_window_control_broker.py"
      - "Tools/ci_v11777_window_control_broker.py"
      - ".github/workflows/build-v11777-window-control-broker.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: windows-latest
    timeout-minutes: 120
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install build dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install pyinstaller requests beautifulsoup4 pypdf cryptography edge-tts winrt-Windows.Media.Control yt-dlp pillow
      - name: Build production Agent Runtime
        run: python Tools/ci_v11777_window_control_broker.py
      - name: Verify v0.11.7.77 window control broker
        shell: python
        run: |
          from pathlib import Path
          b = Path("Bridge/vex_bridge.py").read_text(encoding="utf-8")
          i = Path("Tools/VexAgentRuntimeInstall.py").read_text(encoding="utf-8")
          assert '"agent_runtime_bundle": "0.11.7.77"' in b
          assert 'def _v11777_window_action(' in b
          assert 'parsed.path == "/windows/window-action"' in b
          assert 'close requires confirm=true' in b
          assert 'def _v11776_launch_app(' in b
          assert 'parsed.path == "/windows/apps"' in b
          assert 'pc-memory-star-query-v11775' in b
          assert 'BUNDLE_VERSION = "0.11.7.77"' in i
          assert "standalone_remote.exists()" in i
          compile(b, "Bridge/vex_bridge.py", "exec")
          print("v0.11.7.77 window control broker verified")
      - name: Upload v0.11.7.77 runtime
        uses: actions/upload-artifact@v4
        with:
          name: Vex-Agent-Runtime-v0.11.7.77-WindowControlBroker
          path: Vex-Agent-Runtime-v0.11.7.77-WindowControlBroker.zip
          if-no-files-found: error
          retention-days: 14
