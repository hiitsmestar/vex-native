# VexBridge Bus

Transport-only branch for VexBridge command envelopes.

Files here must contain only authenticated encrypted envelopes or non-sensitive transport metadata. Personal files, continuity text, credentials, and raw command results do not belong here.
